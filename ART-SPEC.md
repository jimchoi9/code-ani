# 애니메이션용 SVG 캐릭터 제작 스펙

이 문서는 외주 제작자나 이미지 생성 도구에서 받은 캐릭터를 현재 SVG+GSAP 리깅 파이프라인에 넣기 위한 최소 납품 계약이다. 완성된 일러스트의 모양뿐 아니라 움직여야 할 파츠의 구조가 납품 품질에 포함된다.

## 좌표와 루트 구조

- 캐릭터의 로컬 원점 `(0,0)`은 양발 사이의 바닥 지점이다.
- 캐릭터 전체는 하나의 루트 `<g id="def-character-name">` 안에 둔다.
- 기본 방향은 오른쪽을 바라보는 방향이다. 반대 방향은 런타임의 scale 반전으로 만든다.
- 파츠의 좌표는 최종 배치 위치가 아니라 캐릭터 로컬 좌표로 작성한다.
- `viewBox`, 원본 크기, 사용한 단위와 기준 캐릭터 높이를 납품 메모에 기록한다.

## 필수 파츠

애니메이션 대상에는 다음 `data-p` 이름을 사용한다.

| 파츠 | 이름 | 요구사항 |
|---|---|---|
| 몸통 | `body` | 다른 움직이는 파츠를 포함하지 않는 몸통 도형 |
| 머리 | `head` | 몸통과 독립적으로 회전 가능 |
| 왼쪽 귀 | `earL` | 머리 안에서 독립 회전 가능 |
| 오른쪽 귀 | `earR` | 머리 안에서 독립 회전 가능 |
| 꼬리 | `tail` | 몸통 연결부를 축으로 독립 회전 가능 |
| 눈 | `eyes` | 깜빡임 또는 시선 이동이 가능하도록 분리 |
| 입 | `mouth` | 표정이나 대사에 맞춰 교체 가능 |
| 장신구 | `bell` | 목걸이, 방울 등 이야기상 움직이는 소품 |

캐릭터에 존재하지 않는 파츠가 있다면 생략하지 말고 “없음”으로 명시한다. 이야기에서 요구하는 파츠가 없다면 아트 승인 전에 보완한다.

```svg
<g id="def-sample">
  <g data-p="tail">...</g>
  <g data-p="body">...</g>
  <g data-p="head">
    <g data-p="earL">...</g>
    <g data-p="earR">...</g>
    <g data-p="eyes">...</g>
    <g data-p="mouth">...</g>
    <g data-face="happy">...</g>
    <g data-face="sad" style="display:none">...</g>
  </g>
  <g data-p="bell">...</g>
</g>
```

## path와 관절 규칙

- 머리와 몸통, 몸통과 꼬리처럼 서로 다른 관절을 하나의 외곽선 path로 묶지 않는다.
- 회전 파츠는 연결부 아래에 충분한 겹침 영역을 둔다. 파츠를 예상 최대 각도로 돌렸을 때 배경이 비치거나 외곽선이 찢어지면 안 된다.
- 눈동자, 눈꺼풀, 입처럼 표정에 필요한 요소를 얼굴 외곽선과 합치지 않는다.
- 자동 트레이싱 결과의 불필요한 점은 정리한다. 과도한 anchor point는 파일 크기뿐 아니라 수정 비용을 높인다.
- stroke를 outline으로 변환했다면 원본 stroke 버전도 함께 보관한다.
- 파츠마다 회전축 좌표를 납품 메모에 기록한다.

## 표정

- 표정 묶음은 `data-face="happy"` 형태의 안정적인 이름을 사용한다.
- 같은 캐릭터의 표정 그룹은 동일한 얼굴 좌표계에 놓아 display 전환만으로 교체할 수 있어야 한다.
- 기본 표정 하나만 표시하고 나머지는 `style="display:none"`으로 숨긴다.
- 눈과 입만 바뀌는 표정이라도 필요한 눈썹, 볼, 눈물 등의 보조 요소를 해당 `data-face` 그룹에 포함한다.

## 허용 기능과 납품 메모

다음 기능은 사용할 수 있지만 반드시 사용 위치와 목적을 기록한다.

- `clipPath`와 `mask`
- SVG filter
- gradient와 pattern
- 임베디드 또는 링크된 래스터 이미지
- 외부 폰트

텍스트는 가능한 한 path로 변환한다. 편집 가능한 텍스트로 남길 경우 프로젝트가 보장하는 폰트 이름과 대체 폰트를 명시한다. 외부 래스터 링크는 상대 경로를 사용하고 파일을 함께 납품한다.

## 납품 전 체크리스트

- [ ] 루트 원점이 발밑 중앙 `(0,0)`이다.
- [ ] `body`, `head`, `earL`, `earR`, `tail`, `eyes`, `mouth`, `bell`의 존재 여부가 기록되어 있다.
- [ ] 각 애니메이션 파츠에 안정적인 `data-p`가 있다.
- [ ] 표정 그룹에 `data-face`가 있다.
- [ ] 머리, 몸통, 꼬리를 가로지르는 통짜 외곽선 path가 없다.
- [ ] 머리와 꼬리를 최대 회전 각도로 돌려도 연결부가 찢어지지 않는다.
- [ ] 눈 깜빡임과 입 교체가 얼굴 외곽선을 다시 그리지 않고 가능하다.
- [ ] filter, mask, clipPath, 래스터 이미지와 폰트 사용 내역이 기록되어 있다.
- [ ] SVG를 단독으로 열었을 때 누락된 자산이나 콘솔 오류가 없다.
- [ ] `characters.js`의 임시 def로 복제해 이동, 좌우 반전, 회전 테스트를 통과했다.

## `new_char.svg`에서 확인된 실패 사례

`new_char.svg`는 자동 트레이싱된 23개 path로 전신 이동과 찌그러짐은 가능하지만 현재 이야기 리그에는 그대로 사용할 수 없다.

- 머리와 몸통 외곽선이 하나의 path여서 고개만 회전하면 외곽선이 찢어진다.
- 꼬리가 없어 기존 꼬리 흔들림 연출을 재현할 수 없다.
- 방울 목걸이는 원본에 없어 별도 덧그림이 필요하다.

따라서 “SVG 파일”이라는 형식만으로 리깅 가능하다고 판단하지 않는다. 위 파츠 계약을 만족하는지가 실제 승인 기준이다.
